
import std.list
import std.io

numbers := [1,2,3]
strings := ["A", "B", "C"]

concat := List::concat(numbers, strings)

IO::println(concat)
IO::println(concat.len)

collon_sep := List::join(strings, ":")

IO::println(collon_sep)

new := collon_sep:concat
IO::println(new)
